version: 2

models:
  - name: dim_customer
    description: Customer dimension
    columns:
      - name: customer_sk
        description: The primary key of the customer generated with row_number
        tests:
          - unique
          - not_null